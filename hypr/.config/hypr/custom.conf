# XWayland scaling fix for HiDPI displays
# This prevents XWayland apps from being blurry on 2x scaled displays
xwayland {
    force_zero_scaling = true
}

# Scale XWayland apps properly with toolkit variables
env = GDK_SCALE,2
env = QT_SCALE_FACTOR,2